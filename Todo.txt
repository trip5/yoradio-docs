MUST:

#define ARDUINO_ESP32S3_DEV

#define USE_BUILTIN_LED   false
#define LED_BUILTIN_S3 255 /* (default is 255). */
LED_BUILTIN_S3

Tips:  around 600

//#define SDSPISPEED 8000000 /* Default speed 4000000 but try 8000000*/


  const popupOverlay = document.getElementById('popup-overlay');
const popupImage = document.getElementById('popup-image');

// Delegated click listener on the #content container
document.getElementById('content').addEventListener('click', (event) =>
{
  const clickedElement = event.target;

  // Check if the clicked element is an image inside #content
  if (clickedElement.tagName === 'IMG')
  {
    popupImage.src = clickedElement.src; // Set popup image source
    popupOverlay.style.display = 'flex'; // Show popup
  }
});

// Close popup when overlay is clicked
popupOverlay.addEventListener('click', () => 
{
  popupOverlay.style.display = 'none'; // Hide popup
});